---
import Layout from '../layouts/Layout.astro';

const socialLinks = [
  {
    platform: 'GitHub',
    url: 'https://github.com/sambulosenda',
    icon: `<svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24"><path d="M12 .297c-6.63 0-12 5.373-12 12 0 5.303 3.438 9.8 8.205 11.385.6.113.82-.258.82-.577 0-.285-.01-1.04-.015-2.04-3.338.724-4.042-1.61-4.042-1.61C4.422 18.07 3.633 17.7 3.633 17.7c-1.087-.744.084-.729.084-.729 1.205.084 1.838 1.236 1.838 1.236 1.07 1.835 2.809 1.305 3.495.998.108-.776.417-1.305.76-1.605-2.665-.3-5.466-1.332-5.466-5.93 0-1.31.465-2.38 1.235-3.22-.135-.303-.54-1.523.105-3.176 0 0 1.005-.322 3.3 1.23.96-.267 1.98-.399 3-.405 1.02.006 2.04.138 3 .405 2.28-1.552 3.285-1.23 3.285-1.23.645 1.653.24 2.873.12 3.176.765.84 1.23 1.91 1.23 3.22 0 4.61-2.805 5.625-5.475 5.92.42.36.81 1.096.81 2.22 0 1.606-.015 2.896-.015 3.286 0 .315.21.69.825.57C20.565 22.092 24 17.592 24 12.297c0-6.627-5.373-12-12-12"/></svg>`,
  },
  {
    platform: 'LinkedIn',
    url: 'https://linkedin.com/in/sambulosenda',
    icon: `<svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24"><path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/></svg>`,
  },
  {
    platform: 'Twitter',
    url: 'https://twitter.com/sambulosenda',
    icon: `<svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24"><path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"/></svg>`,
  },
];

const technologies = [
  { name: 'React Native', primary: true },
  { name: 'TypeScript', primary: true },
  { name: 'Redux', primary: false },
  { name: 'GraphQL', primary: false },
  { name: 'Node.js', primary: false },
  { name: 'CI/CD', primary: false },
];

const apps = [
  {
    title: 'Softmind',
    description: 'AI-powered mental wellness app with mood tracking, guided check-ins, journaling, and personalized programs.',
    tags: ['React Native', 'AI', 'Personal Project'],
    screenshot: '/softmind.jpeg',
    url: 'https://getsoftmind.com',
  },
  {
    title: 'Andersen EV',
    description: 'Migrated EV charging app to React Native New Architecture, improving performance by 30%.',
    tags: ['React Native', 'IoT', 'Performance'],
    screenshot: '/andersen-ev.png',
  },
  {
    title: 'Aspire',
    description: 'Built mental well-being app serving 10,000+ users with 98% crash-free rate.',
    tags: ['React Native', 'MobX', 'GraphQL'],
    screenshot: '/aspire.png',
  },
  {
    title: 'Report Focus News',
    description: 'Offline-first architecture with biometric auth and GDPR compliance.',
    tags: ['Consulting', 'AWS', 'Security'],
    screenshot: '/report-focus-news.png',
  },
];

---

<Layout title="Sambulo Senda — React Native Developer">
  <main class="bg-white dark:bg-zinc-950">

    <!-- Hero -->
    <section class="min-h-screen flex items-center pt-20 pb-16 lg:pt-0 lg:pb-0">
      <div class="max-w-6xl mx-auto px-6 w-full">
        <div class="grid lg:grid-cols-5 gap-12 lg:gap-16 items-center">

          <!-- Content - takes more space -->
          <div class="lg:col-span-3 space-y-6">
            <!-- Eyebrow -->
            <p class="text-sm tracking-widest text-zinc-400 uppercase animate-fade-in">
              React Native Developer
            </p>

            <!-- Main headline - larger, more impactful -->
            <h1 class="text-5xl md:text-6xl lg:text-7xl font-medium text-zinc-900 dark:text-white leading-[1.1] tracking-tight animate-fade-in-up delay-1">
              I build apps<br/>
              <span class="text-zinc-300 dark:text-zinc-600">people love.</span>
            </h1>

            <!-- Subheadline -->
            <p class="text-xl text-zinc-500 leading-relaxed max-w-lg animate-fade-in-up delay-2">
              Helping teams create beautiful, high-performance mobile experiences with React Native.
            </p>

            <!-- CTAs -->
            <div class="flex flex-wrap gap-4 animate-fade-in-up delay-4">
              <a
                href="https://cal.com/sambulosenda"
                target="_blank"
                class="px-6 py-3 bg-zinc-900 dark:bg-white text-white dark:text-zinc-900 rounded-full text-sm font-medium hover:bg-zinc-700 dark:hover:bg-zinc-200 transition-all hover:scale-105"
              >
                Book a call
              </a>
              <a
                href="#work"
                class="px-6 py-3 border border-zinc-200 dark:border-zinc-800 text-zinc-600 dark:text-zinc-400 rounded-full text-sm font-medium hover:bg-zinc-50 dark:hover:bg-zinc-900 transition-all"
              >
                View work
              </a>
            </div>

            <!-- Social -->
            <div class="flex items-center gap-5 pt-2 animate-fade-in-up delay-5">
              {socialLinks.map(({ url, platform, icon }) => (
                <a
                  href={url}
                  target="_blank"
                  class="text-zinc-300 hover:text-zinc-900 dark:text-zinc-600 dark:hover:text-white transition-colors"
                  aria-label={platform}
                >
                  <Fragment set:html={icon} />
                </a>
              ))}
            </div>
          </div>

          <!-- Profile Image - smaller, balanced -->
          <div class="lg:col-span-2 animate-fade-in-up delay-3">
            <div class="relative">
              <!-- Status badge -->
              <div class="absolute -top-3 -right-3 z-10 flex items-center gap-2 px-3 py-1.5 bg-white dark:bg-zinc-900 border border-zinc-100 dark:border-zinc-800 rounded-full shadow-lg animate-fade-in delay-6">
                <span class="w-2 h-2 bg-emerald-500 rounded-full animate-pulse"></span>
                <span class="text-xs font-medium text-zinc-600 dark:text-zinc-400">Available</span>
              </div>

              <div class="aspect-[3/4] rounded-3xl overflow-hidden bg-zinc-100 dark:bg-zinc-900 ring-1 ring-zinc-100 dark:ring-zinc-800">
                <img
                  src="/profile.jpg"
                  alt="Sambulo Senda"
                  class="w-full h-full object-cover"
                />
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Tech -->
    <section class="py-16 border-y border-zinc-100 dark:border-zinc-900">
      <div class="max-w-6xl mx-auto px-6">
        <div class="flex flex-wrap justify-center gap-3">
          {technologies.map((tech) => (
            <span class={`px-4 py-2 rounded-full text-sm transition-all hover:scale-105 ${
              tech.primary
                ? 'bg-zinc-900 dark:bg-white text-white dark:text-zinc-900 font-medium'
                : 'bg-zinc-50 dark:bg-zinc-900 border border-zinc-100 dark:border-zinc-800 text-zinc-500'
            }`}>
              {tech.name}
            </span>
          ))}
        </div>
      </div>
    </section>

    <!-- Work -->
    <section id="work" class="py-24 lg:py-32">
      <div class="max-w-6xl mx-auto px-6">
        <!-- Section header -->
        <div class="max-w-2xl mb-16">
          <p class="text-sm tracking-widest text-zinc-400 uppercase mb-4">Portfolio</p>
          <h2 class="text-4xl lg:text-5xl font-medium text-zinc-900 dark:text-white tracking-tight mb-4">
            Selected Work
          </h2>
          <p class="text-lg text-zinc-500">
            Apps designed and developed from concept to App Store launch.
          </p>
        </div>

        <!-- Bento Grid -->
        <div class="grid grid-cols-1 md:grid-cols-2 gap-5">
          <!-- Featured (Softmind) -->
          <article class="group animate-on-scroll" style="--delay: 0ms">
            <div class="relative aspect-[4/5] rounded-2xl overflow-hidden bg-zinc-100 dark:bg-zinc-900 ring-1 ring-zinc-100 dark:ring-zinc-800">
              <img
                src={apps[0].screenshot}
                alt={apps[0].title}
                class="w-full h-full object-cover object-top transition-transform duration-700 group-hover:scale-105"
                loading="lazy"
              />
              <div class="absolute inset-0 bg-gradient-to-t from-zinc-900/80 via-zinc-900/20 to-transparent"></div>
              <div class="absolute bottom-0 left-0 right-0 p-6">
                <div class="flex gap-2 text-xs text-zinc-300 mb-2">
                  {apps[0].tags.map((tag, i) => (
                    <span>{tag}{i < apps[0].tags.length - 1 ? ' ·' : ''}</span>
                  ))}
                </div>
                <h3 class="text-2xl font-medium text-white mb-2">{apps[0].title}</h3>
                <p class="text-zinc-300 text-sm max-w-md">{apps[0].description}</p>
              </div>
            </div>
          </article>

          <!-- Card 2 (Andersen EV) -->
          <article class="group animate-on-scroll" style="--delay: 100ms">
            <div class="relative aspect-[4/5] rounded-2xl overflow-hidden bg-zinc-100 dark:bg-zinc-900 ring-1 ring-zinc-100 dark:ring-zinc-800">
              <img
                src={apps[1].screenshot}
                alt={apps[1].title}
                class="w-full h-full object-cover transition-transform duration-700 group-hover:scale-105"
                loading="lazy"
              />
              <div class="absolute inset-0 bg-gradient-to-t from-zinc-900/80 via-zinc-900/20 to-transparent"></div>
              <div class="absolute bottom-0 left-0 right-0 p-6">
                <div class="flex gap-2 text-xs text-zinc-300 mb-2">
                  {apps[1].tags.map((tag, i) => (
                    <span>{tag}{i < apps[1].tags.length - 1 ? ' ·' : ''}</span>
                  ))}
                </div>
                <h3 class="text-2xl font-medium text-white mb-2">{apps[1].title}</h3>
                <p class="text-zinc-300 text-sm max-w-md">{apps[1].description}</p>
              </div>
            </div>
          </article>

          <!-- Card 3 (Aspire) -->
          <article class="group animate-on-scroll" style="--delay: 200ms">
            <div class="relative aspect-[4/5] rounded-2xl overflow-hidden bg-zinc-100 dark:bg-zinc-900 ring-1 ring-zinc-100 dark:ring-zinc-800">
              <img
                src={apps[2].screenshot}
                alt={apps[2].title}
                class="w-full h-full object-cover transition-transform duration-700 group-hover:scale-105"
                loading="lazy"
              />
              <div class="absolute inset-0 bg-gradient-to-t from-zinc-900/80 via-zinc-900/20 to-transparent"></div>
              <div class="absolute bottom-0 left-0 right-0 p-6">
                <div class="flex gap-2 text-xs text-zinc-300 mb-2">
                  {apps[2].tags.map((tag, i) => (
                    <span>{tag}{i < apps[2].tags.length - 1 ? ' ·' : ''}</span>
                  ))}
                </div>
                <h3 class="text-2xl font-medium text-white mb-2">{apps[2].title}</h3>
                <p class="text-zinc-300 text-sm max-w-md">{apps[2].description}</p>
              </div>
            </div>
          </article>

          <!-- Card 4 (Report Focus News) -->
          <article class="group animate-on-scroll" style="--delay: 300ms">
            <div class="relative aspect-[4/5] rounded-2xl overflow-hidden bg-zinc-100 dark:bg-zinc-900 ring-1 ring-zinc-100 dark:ring-zinc-800">
              <img
                src={apps[3].screenshot}
                alt={apps[3].title}
                class="w-full h-full object-cover transition-transform duration-700 group-hover:scale-105"
                loading="lazy"
              />
              <div class="absolute inset-0 bg-gradient-to-t from-zinc-900/80 via-zinc-900/20 to-transparent"></div>
              <div class="absolute bottom-0 left-0 right-0 p-6">
                <div class="flex gap-2 text-xs text-zinc-300 mb-2">
                  {apps[3].tags.map((tag, i) => (
                    <span>{tag}{i < apps[3].tags.length - 1 ? ' ·' : ''}</span>
                  ))}
                </div>
                <h3 class="text-2xl font-medium text-white mb-2">{apps[3].title}</h3>
                <p class="text-zinc-300 text-sm max-w-md">{apps[3].description}</p>
              </div>
            </div>
          </article>
        </div>
      </div>
    </section>

    <!-- CTA -->
    <section class="py-24 lg:py-32 bg-zinc-950">
      <div class="max-w-3xl mx-auto px-6 text-center">
        <p class="text-sm tracking-widest text-zinc-500 uppercase mb-6">Get in touch</p>
        <h2 class="text-4xl lg:text-5xl font-medium text-white tracking-tight mb-6">
          Let's build something<br/>great together
        </h2>
        <p class="text-lg text-zinc-400 mb-10 max-w-xl mx-auto">
          Have a project in mind? I'd love to hear about it. Let's discuss how I can help bring your ideas to life.
        </p>
        <div class="flex flex-wrap gap-4 justify-center">
          <a
            href="https://cal.com/sambulosenda"
            target="_blank"
            class="px-8 py-4 bg-white text-zinc-900 rounded-full font-medium hover:bg-zinc-100 transition-all hover:scale-105"
          >
            Schedule a call
          </a>
          <a
            href="mailto:sambulosenda@gmail.com"
            class="px-8 py-4 border border-zinc-700 text-zinc-300 rounded-full font-medium hover:bg-zinc-800 transition-all"
          >
            Send an email
          </a>
        </div>
      </div>
    </section>

    <!-- Footer -->
    <footer class="py-10 border-t border-zinc-100 dark:border-zinc-900">
      <div class="max-w-6xl mx-auto px-6">
        <div class="flex flex-col md:flex-row justify-between items-center gap-6 text-sm text-zinc-400">
          <span>© {new Date().getFullYear()} Sambulo Senda</span>
          <div class="flex gap-8">
            {socialLinks.map(({ url, platform }) => (
              <a href={url} target="_blank" class="hover:text-zinc-900 dark:hover:text-white transition-colors">
                {platform}
              </a>
            ))}
          </div>
        </div>
      </div>
    </footer>
  </main>
</Layout>

<style>
  /* Animations */
  @keyframes fade-in {
    from { opacity: 0; }
    to { opacity: 1; }
  }

  @keyframes fade-in-up {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-fade-in {
    animation: fade-in 0.6s ease-out forwards;
  }

  .animate-fade-in-up {
    animation: fade-in-up 0.6s ease-out forwards;
  }

  .delay-1 { animation-delay: 100ms; opacity: 0; }
  .delay-2 { animation-delay: 200ms; opacity: 0; }
  .delay-3 { animation-delay: 300ms; opacity: 0; }
  .delay-4 { animation-delay: 400ms; opacity: 0; }
  .delay-5 { animation-delay: 500ms; opacity: 0; }
  .delay-6 { animation-delay: 600ms; opacity: 0; }

  /* Scroll animations */
  .animate-on-scroll {
    opacity: 0;
    transform: translateY(30px);
    transition: opacity 0.6s ease-out, transform 0.6s ease-out;
    transition-delay: var(--delay, 0ms);
  }

  .animate-on-scroll.visible {
    opacity: 1;
    transform: translateY(0);
  }
</style>

<script>
  // Intersection Observer for scroll animations
  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.classList.add('visible');
        }
      });
    },
    { threshold: 0.1, rootMargin: '0px 0px -50px 0px' }
  );

  document.querySelectorAll('.animate-on-scroll').forEach((el) => {
    observer.observe(el);
  });

  // Smooth scroll
  document.querySelectorAll('a[href^="#"]').forEach((anchor) => {
    anchor.addEventListener('click', function (e) {
      e.preventDefault();
      const target = document.querySelector(this.getAttribute('href'));
      if (target) {
        target.scrollIntoView({ behavior: 'smooth' });
      }
    });
  });
</script>
